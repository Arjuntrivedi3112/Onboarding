# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables (contains secrets)
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vendored design reference (installed at .claude/skills/apple-design)
apple-design-skill-main/

# IQM's internal design system reference (V2.5 skill files, not app source)
Product Design Skills/

# Generated content search index — rebuilt by predev/prebuild from the
# actual lesson source, so it's always fresh and never worth committing.
/public/search-index.json
